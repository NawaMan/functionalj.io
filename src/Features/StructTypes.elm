module Features.StructTypes exposing (featStructTypes)

import CodeBlock exposing (codeBlock)
import Feature exposing (..)
import Html exposing (Html, button, code, div, h1, p, span, text)
import Html.Attributes exposing (class, id)


featStructTypes =
    Feature "Struct - Immutable Data"
        """
        Struct let us create custom immutable data type.
        These generated types has automatically generated lens, exhaust builder and quick validatiion.
        """
        [ -- 0
          [ p []
                [ text "To be added"
                ]
          , codeShow """
    // To be added
    """
          ]
        ]

